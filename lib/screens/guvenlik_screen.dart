import 'package:flutter/material.dart';

class GuvenlikScreen extends StatelessWidget {
  const GuvenlikScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Güvenlik Ayarları'),
      ),
    );
  }
}
